import { DataTableEditableRowDirective } from './data-table-editable-row.directive';

describe('DataTableEditableRowDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTableEditableRowDirective();
    expect(directive).toBeTruthy();
  });
});
